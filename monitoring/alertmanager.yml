route:
  receiver: default
  group_by: ["alertname"]
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 1h

receivers:
  - name: default
    # No external integration wired by default. Add a Slack/webhook config here,
    # e.g. slack_configs or webhook_configs, to deliver alerts downstream.
